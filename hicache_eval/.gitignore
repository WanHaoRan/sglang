# The repo root ignores *.log, *.csv, *.png and *.jsonl. Inside this directory
# those files are the measured evidence behind REPORT.md, not build output, so
# re-include them. Patterns in a nested .gitignore override the parent's.
!*.log
!*.csv
!*.png
!*.jsonl
